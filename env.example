# Backend API URL (optional - defaults to http://localhost:8000 for local dev, or Render URL for production)
REACT_APP_API_URL=http://localhost:8000

# Backend Environment Variables
# Storage mode: using local Python file server (Firebase removed)
STORAGE_MODE=local

# Database Configuration (for backend)
DATABASE_URL=postgresql://user:password@localhost:5432/fog_platform

# Secret Key for JWT tokens (generate a secure random string)
SECRET_KEY=your-secret-key-here-change-in-production

# CORS Origins (comma-separated list of allowed origins)
CORS_ORIGINS=http://localhost:3000,https://familyofgreatness.com,https://www.familyofgreatness.com

# Environment
ENVIRONMENT=development
